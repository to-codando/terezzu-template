export default (root, props) =>   /*css*/`
        ${root},
        ${root} .text-wrapper {
            display:flex;
            align-items:center;
            justify-content:center;
            width:100%;
            margin:15px 0
        }
    `